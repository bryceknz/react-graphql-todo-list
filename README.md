# Basic todo list using React+GraphQL (Apollo)

## Scripts
`yarn server` to run GraphQL API

`yarn start` to run React app


## Future
* Allow user to toggle completion
* Allow user to add todo item
* Allow user to remove todo item
